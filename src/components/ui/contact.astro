---
import { Check, Mail, Phone, SeparatorHorizontal } from "@lucide/astro";
import Badge from "./badge.astro";
import { Card } from "./card";
import { Button } from "./button";
---

<section
    id="contacto"
    class="py-20 px-6 bg-muted border-b from-primary/5 to-primary/10"
>
    <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
            <Badge class="mb-4"> 📞 Contáctanos </Badge>
            <h3 class="text-4xl font-bold text-primary mb-4">
                ¿Listo para comenzar tu proyecto?
            </h3>
            <p class="text-xl text-muted-foreground">
                Estamos aquí para convertir tus ideas en realidad digital.
            </p>
        </div>

        <Card className="p-8 bg-white/80 backdrop-blur-sm border-0 shadow-xl">
            <div class="grid md:grid-cols-2 gap-8">
                <div class="space-y-6">
                    <div class="flex items-center space-x-4">
                        <div class="bg-green-100 p-3 rounded-lg">
                            <Phone class="h-6 w-6 text-green-600" />
                        </div>
                        <div>
                            <p class="font-semibold text-primary">Teléfono</p>
                            <p class="text-muted-foreground">+51 916 225 416</p>
                        </div>
                    </div>

                    <div class="flex items-center space-x-4">
                        <div class="bg-blue-100 p-3 rounded-lg">
                            <Mail class="h-6 w-6 text-blue-600" />
                        </div>
                        <div>
                            <p class="font-semibold text-primary">Email</p>
                            <p class="text-muted-foreground">
                                securaq0@gmail.com
                            </p>
                        </div>
                    </div>
                </div>

                <div class="space-y-4">
                    <h4 class="font-semibold text-primary mb-4">
                        ¿Por qué elegirnos?
                    </h4>
                    <div class="space-y-3">
                        <div class="flex items-center space-x-3">
                            <Check class="h-5 w-5 text-green-500" />
                            <span class="text-sm text-muted-foreground"
                                >Desarrollo personalizado</span
                            >
                        </div>
                        <div class="flex items-center space-x-3">
                            <Check class="h-5 w-5 text-green-500" />
                            <span class="text-sm text-muted-foreground"
                                >Seguridad garantizada</span
                            >
                        </div>
                        <div class="flex items-center space-x-3">
                            <Check class="h-5 w-5 text-green-500" />
                            <span class="text-sm text-muted-foreground"
                                >Entrega rápida y efectiva</span
                            >
                        </div>
                        <div class="flex items-center space-x-3">
                            <Check class="h-5 w-5 text-green-500" />
                            <span class="text-sm text-muted-foreground"
                                >Soporte académico especializado</span
                            >
                        </div>
                    </div>
                </div>
            </div>

            <hr class="my-8" />

            <div class="text-center">
                <Button
                    size="lg"
                    className="bg-primary hover:bg-primary/90 px-8"
                >
                    <Phone class="mr-2 h-5 w-5" />
                    Iniciar conversación
                </Button>
            </div>
        </Card>
    </div>
</section>
