---
import { Phone, Shield, Star, TrendingUp } from "@lucide/astro";
import { Button } from "./button";
import Badge from "./badge.astro";
const features = [
    {
        icon: Shield,
        text: "Seguridad y calidad garantizadas",
    },
    {
        icon: TrendingUp,
        text: "Soluciones que impulsan tu negocio",
    },
    {
        icon: Star,
        text: "Resultados efectivos y rápidos",
    },
];
---

<section class="relative px-6 w-full border-b py-12 md:py-20">
    <div class="max-w-6xl mx-auto text-center">
        <Badge class="mb-6 px-4 py-2">
            💻 Desarrollo de software y soluciones a medida
        </Badge>
        <h2
            class="text-5xl md:text-6xl font-bold text-primary mb-6 leading-tight"
        >
            Tu idea, nuestro código
        </h2>

        <p class="text-xl text-muted-foreground mb-8 max-w-2xl mx-auto">
            <span class="font-semibold text-primary"
                >Segura, rápida, efectiva.</span
            >
            {" "}Transformamos tus ideas en soluciones tecnológicas que impulsan
            tu negocio hacia el éxito.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
            <Button size="lg" className="bg-primary hover:bg-primary/90 px-8">
                <Phone class="mr-2 h-5 w-5" />
                Contactar ahora
            </Button>
            <Button size="lg" variant="outline" className="px-8">
                Ver servicios
            </Button>
        </div>

        <div class="grid md:grid-cols-3 gap-6 max-w-3xl mx-auto">
            {
                features.map((feature) => (
                    <div class="flex items-center justify-center space-x-3 text-muted-foreground">
                        <div class="text-primary">
                            <feature.icon class="h-6 w-6" />
                        </div>
                        <span class="text-sm font-medium">{feature.text}</span>
                    </div>
                ))
            }
        </div>
    </div>
</section>
